{
  "name": "escrow",
  "version": "1.0.0",
  "description": "Escrow Smart Contracts",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "npx truffle compile --all",
    "migrate-bsct": "npx truffle migrate --network bsct",
    "verify-EscrowFactory-bsct": "npx truffle run verify EscrowFactory --network bsct",
    "verify-Escrow-bsct": "npx truffle run verify Escrow --network bsct"
  },
  "author": "Keval Sayar",
  "license": "ISC",
  "devDependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "@openzeppelin/truffle-upgrades": "^1.17.0",
    "@truffle/hdwallet-provider": "^2.1.0",
    "dotenv": "^16.0.3",
    "truffle": "^5.6.1",
    "truffle-plugin-verify": "^0.6.1"
  }
}